{
  "name": "cetmed",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n CMS,APP -c cyan,green \"npm run develop --prefix cms\" \"npm run dev --prefix frontend\"",
    "cms": "npm run develop --prefix cms",
    "app": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix frontend && npm install --prefix cms"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
